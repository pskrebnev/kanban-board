// Shared Tailwind class strings for the authentication screens (login, sign-up,
// verification, forgot/reset password). One place so the card layout stays
// consistent without repeating long utility lists.

export const authShell = "flex min-h-screen items-center justify-center p-6";

export const authCard =
  "w-full max-w-[420px] rounded-2xl border border-line bg-white p-10 shadow-[0_24px_60px_rgb(23_32_51/10%)]";

export const authTitle = "mb-6 text-[1.75rem] font-bold";

export const authForm = "flex flex-col gap-4";

export const authLabel = "flex flex-col gap-1.5 font-bold";

export const authInput =
  "rounded-lg border border-field px-3.5 py-2.5 font-[inherit] font-normal focus:border-brand focus:outline-none focus:ring-2 focus:ring-brand/25";

export const authAlt = "mt-6";
